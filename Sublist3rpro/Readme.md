$ This script finds subdomains, their IP addresses, their NetName, NetRange and CIDR. 

$ All you have to do is enter a domain name.

$ You'll have to take your API key from https://api.securitytrails.com/
